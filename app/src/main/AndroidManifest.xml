<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      WorkManager must NOT initialise itself. SyncBeneficiariesWorker has an injected
      dependency, so it needs HiltWorkerFactory, which AstraCareApplication supplies via
      Configuration.Provider. That only takes effect if the framework has not already started
      with its default factory.

      Removing the initializer below is the step everyone forgets. Without it the build is
      green and the app crashes the first time a worker runs, with a message naming the worker
      rather than these four lines.
    -->
    <!--
      allowBackup="false". Day 16.

      Encrypting the database at rest and then letting Auto Backup copy it to a consumer cloud
      account would undo most of the point: the backup transport is outside this app's threat
      model and the records are a health worker's beneficiaries, some of which have never
      reached a server. The Keystore-wrapped passphrase cannot leave the device, so a restored
      copy would not even open — unwanted first, useless second.

      The two rules files are filled in as well, not left as the template's commented-out
      samples. allowBackup does not govern Android 12+ device-to-device transfer, which is a
      separate channel with its own opt-out.
    -->
    <application
        android:name=".AstraCareApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Astracare">
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Astracare"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>